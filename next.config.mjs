/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { domains: ["res.cloudinary.com"] }, // Add your Cloudinary domain here"https://res.cloudinary.com",
};

export default nextConfig;
